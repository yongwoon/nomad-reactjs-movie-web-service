# Set develop env

- Build docker images

```bash
docker-compose build --no-cache
```

- install package

```bash
docker-compose run --rm app npm install
```

- Run docker container

```bash
docker-compose up
```
